# Gentleman's Wager

Monorepo for the Roeto home assignment. See `ARCHITECTURE.md` for the
full design (decisions, trade-offs, what's deferred and why).

- `backend-api` — NestJS backend. All game rules live here (see its own README to run it).
- `frontend` — React + Vite UI. Not built yet.
