# Base URL the frontend calls for the backend API. Baked in at build time
# (Vite env vars are compile-time), so the Docker build passes this as a
# build ARG — see ../docker-compose.yml and Dockerfile.
VITE_API_BASE_URL=http://localhost:3000
