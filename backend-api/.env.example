DATABASE_URL="file:./dev.db"
JWT_SECRET="change-me-in-production"
REDIS_URL="redis://localhost:6379"
PORT=3000

# Optional. How long the cached leaderboard list lives in Redis, in
# seconds. Defaults to 15 if unset — see README "Leaderboard pagination".
LEADERBOARD_TTL_SECONDS=
